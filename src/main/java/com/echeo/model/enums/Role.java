package com.echeo.model.enums;

/**
 * Rôle applicatif d'un utilisateur, utilisé par Spring Security
 * pour la définition des autorités (ROLE_USER / ROLE_ADMIN).
 */
public enum Role {
    USER,
    ADMIN
}
