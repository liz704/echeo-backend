spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/echeo1
    username: echeo_user
    password: secret123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

echeo:
  jwt:
    secret: "change-moi-avec-une-valeur-longue-et-aleatoire-min-32-caracteres"
    expiration-ms: 86400000
  mail:
    from: chuembouketchemenlizperle.com
    from-name: "ÉCHÉO"
  brevo:
    api-key: "xsmtpsib-473a57b539c1a66999f6def3d86f797ee52563cad55d0143d08e4f209cedd9db-ZOBa278wfLcNnWKa"   # Brevo > Settings > SMTP & API > API Keys
  frontend:
    base-url: http://localhost:3000

management:
  endpoints:
    web:
      exposure:
        include: health

server:
  port: ${PORT:8080}